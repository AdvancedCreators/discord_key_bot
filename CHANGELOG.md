# 変更履歴

## ver2

前回コミット (`b7bdca4`) からのリマインド機能まわりの変更点。

### daily（返却催促）リマインド

- デフォルト時刻を **20 時 → 21 時** に変更（`_DEFAULT_DAILY_HOUR`）。
- 発火条件を見直し:
  - 時刻判定を `now.hour == daily_hour` → **`now.hour >= daily_hour`（設定時刻以降）** に変更。
  - **未返却（`closed`=閉めた / `out`=持ち出し中）のまま 30 分以上経過** していることを条件に追加（`_DAILY_CLOSED_GRACE_MINUTES = 30` を新設）。
  - → 「**設定時刻以降に、未返却のまま 30 分以上経過していたら 1 日 1 回**、`鍵を返し忘れていませんか？` を送信」。
- `/reminder_daily`:
  - 時刻をプルダウン（選択肢）方式に変更。デフォルト表記を 21 時に。
  - 説明文・完了メッセージを「未返却のまま 30 分経過後に送信」に更新。

### idle（場所未報告）リマインド

- 送信条件に **`state == "closed"`（閉めて場所未報告）** を明示。
- 通知文を `{n}時間 状態に変化がありません。` → **`鍵を閉めてから{n}時間経過しています。場所の報告を忘れていませんか？`** に変更。
- `/reminder_idle`:
  - 時間をプルダウン（選択肢）方式に変更（デフォルト 2 時間）。
  - 説明文を「無変化」→「鍵を閉めて場所未報告のときのリマインド時間」に更新。

### /reminder_status

- フィールド名を「毎日リマインド」→「**返却催促リマインド**」、「無変化リマインド」→「**場所未報告リマインド**」に変更。
- 各設定値の表示を新しい挙動に合わせて更新（daily: `{時}時以降・未返却30分後` / idle: `閉めて場所未報告で {時間}時間`）。
- 長期貸出時の文言をハードコードの「20 時」から設定値（`daily_hour`）参照に変更。
- 「次回 daily 予定」を新ロジック（未返却なら `閉/持出時刻 + 30分` と `daily_hour 時` の遅い方、未返却でなければ案内表示）に書き換え。
- 「次回 idle 予定」を `state == "closed"` のときのみ表示するよう調整。

### /debug_reminder

- idle テスト通知の文面を本番と同じ `鍵を閉めてから{n}時間経過しています。場所の報告を忘れていませんか？` に更新。
